# Netlify configuration for monorepo structure
# The site is located in the 'frontend' directory

[build]
  base = "frontend"
  command = "npm run build"
  publish = ".next"

# Essential Next.js plugin for Netlify
[[plugins]]
  package = "@netlify/plugin-nextjs"
